# CharacterLoraPreset
A ComfyUI custom node for managing character LoRAs. Save a character once — its LoRA file, trigger word, and description — and load it into any prompt with a single click. Mix multiple characters in one generation, tweak strengths per-run without touching your saved presets, and keep a large character library organized without cluttering.
